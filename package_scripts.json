{
  "name": "unihomes-server",
  "version": "1.0.0",
  "description": "Backend per FuoriSede/UniHomes - Piattaforma affitti per studenti",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "db:sync": "node syncDatabase.js",
    "db:init": "node syncDatabase.js --samples",
    "db:reset": "node -e \"require('./syncDatabase').syncDatabase().then(() => process.exit())\"",
    "test": "echo \"Test suite da implementare\" && exit 1"
  },
  "keywords": ["nodejs", "express", "postgresql", "sequelize", "real-estate", "student-housing"],
  "author": "Team FuoriSede",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "bcrypt": "^5.1.0",
    "sequelize": "^6.32.1",
    "pg": "^8.11.1",
    "pg-hstore": "^2.3.4",
    "multer": "^1.4.5-lts.1",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}